# Todo
This folder should be the endpoints for WebSocket connections, because API and WebSocket's cannot use the same http paths.

Example would be reserving `/` (root) for WebSocket connections. That means the API cannot use `/` for GET / POST requests.

