# Routes (API)
This folder is for API routes, which are used to handle GET / POST requests, etc.

Example would be `/api/users` for getting all users, or `/api/users/:id` for getting a specific user.
Any API Routes overrides WebSocket endpoints.